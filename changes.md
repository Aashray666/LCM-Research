# Changes Required for Publishable Results

This document tracks what the current pipeline run achieved, what its limitations are, and exactly what needs to be done before results can be submitted to a conference or journal.

---

## Current State (May 2026 Run)

| Parameter | Current Value | Problem |
|---|---|---|
| Training epochs | **1** | Model has not converged — 1 epoch is insufficient |
| Training pairs | **6,000** | Target reached. Previously 1,998 (~8% of chunks). |
| Random seeds tested | **1** (seed 42 only) | Single run — no variance estimate |
| Training device | **CPU** | Slow; no GPU acceleration used |
| External benchmark | **Skipped** | BEIR/NFCorpus not evaluated |

The current MRR of **0.709** (fine-tuned) vs **0.636** (baseline) is a promising signal, but it is a **proof-of-concept result, not a publication-ready claim**, for the reasons below.

---

## Why These Results Are Not Yet Publishable

### 1. 1 Epoch = Underfit Model

`MultipleNegativesRankingLoss` (MNRL) typically requires **3–5 epochs** to converge when fine-tuning sentence transformer models. Running for only 1 epoch means:
- The model has seen each training pair only once
- Gradient updates are minimal relative to the model's capacity
- The **true performance ceiling is unknown** — the 0.709 MRR is a floor estimate

**What reviewers will ask:** *"Did the model converge? What does the training loss curve look like across epochs?"*

### 2. 1,998 Pairs Is Borderline

With 24,166 chunks available, 1,998 training pairs (~8% coverage) is workable but thin. Comparable published retrieval fine-tuning experiments typically use:
- BEIR training sets: 5,000–50,000 pairs
- Domain-adapted models: 3,000–10,000 pairs minimum

The risk is that the model overfits a narrow slice of the biomedical vocabulary rather than learning robust domain-level representations.

**What reviewers will ask:** *"How sensitive are results to the number of training pairs? Did you run an ablation?"*

### 3. No Variance Estimate (Single Seed)

A single fine-tuning run can be lucky or unlucky depending on the random initialization and batch ordering. Standard practice for any ML paper is:
- Run **3 seeds** minimum (e.g., seeds 42, 43, 44)
- Report **mean ± standard deviation** for all metrics
- Ensure the improvement is statistically consistent, not a one-off

**What reviewers will ask:** *"Is this improvement statistically significant? What is the variance across runs?"*

### 4. No External Benchmark Evaluation

The current evaluation uses **synthetic test pairs** generated by the same LLM that generated training pairs. This means:
- The test distribution partially matches the training distribution
- Results may be inflated relative to real user queries
- No connection to established biomedical IR benchmarks

Running on **BEIR/NFCorpus** or **BioASQ** provides an independent, distribution-shifted evaluation that reviewers will expect.

**What reviewers will ask:** *"How does the model perform on standard benchmarks outside your own synthetic test set?"*

### 5. No Ablation Study

The paper currently cannot answer:
- Does MNRL help more than TripletLoss? (yes, by design, but needs empirical support)
- Does the number of epochs matter? (unknown)
- Does the number of training pairs matter? (unknown)
- Do hard negatives help over random negatives? (unknown)

Ablations are standard in retrieval fine-tuning papers and make the contribution much stronger.

---

## What Needs to Be Done

### Step 1 — Increase Training Data (High Priority)

**Target:** 4,000–6,000 training pairs (from current 1,998)

**How:** In `CONFIG` in the notebook, change:
```python
"train_pairs_target": 6000,
"test_pairs_target":  800,
```
Then re-run **Cell 9 only** — the pipeline will resume from pair 1,998 and generate the remaining pairs automatically. Takes ~3–4 hours overnight using NVIDIA NIM.

**Why:** Doubles coverage of biomedical subdomains and gives the model more gradient signal.

---

### Step 2 — Increase Training Epochs (High Priority)

**Target:** 3–5 epochs

**How:** In `CONFIG`, change:
```python
"training_epochs": 3,
```
Then re-run **Cell 11 only** (fine-tuning). No data regeneration needed.

**Why:** MNRL convergence requires multiple passes. 3 epochs is the minimum for a credible claim.

---

### Step 3 — Run 3 Seeds and Report Variance (High Priority)

**Target:** Run full fine-tune + evaluate (Cells 11–13) with 3 different seeds.

**How:** After generating enough pairs (Step 1), run Cells 11–13 three times with:
```python
"random_seed": 42   # Run 1
"random_seed": 43   # Run 2
"random_seed": 44   # Run 3
```
Average the MRR, NDCG@10, and Recall@10 across the 3 runs and compute standard deviation.

**Why:** Establishes statistical confidence in the improvement.

---

### Step 4 — External Benchmark Evaluation (Medium Priority)

**Target:** Evaluate all 4 models on BEIR/NFCorpus or BioASQ

**How:** In `CONFIG`, set:
```python
"run_external_benchmark_evaluation": True,
```
Then re-run **Cell 14 only**. Requires internet connection and will download BEIR datasets automatically.

**Why:** Provides independent validation on real (non-synthetic) queries. Critical for credibility.

---

### Step 5 — Ablation Study (Medium Priority)

Run additional fine-tuning experiments to compare:

| Experiment | Change |
|---|---|
| No hard negatives | Set `training_use_hard_negatives: False` |
| TripletLoss vs MNRL | Swap loss function in Cell 22 |
| 1 epoch vs 3 epochs vs 5 epochs | Vary `training_epochs` |
| 2K pairs vs 4K pairs vs 6K pairs | Vary `train_pairs_target` |

These ablations can be run in parallel and form the ablation table in the paper.

---

## Summary Checklist

- [x] **Generate 6,000 training pairs** (currently 1,998) — 6000 train / 793 test pairs generated
- [ ] **Train for 3 epochs** (currently 1) — change CONFIG, re-run Cell 11
- [ ] **Run 3 seeds** — run Cells 11–13 with seeds 42, 43, 44, report mean ± std
- [ ] **Run external BEIR benchmark** — set `run_external_benchmark_evaluation: True`, run Cell 14
- [ ] **Ablation table** — vary epochs, pairs, loss function, hard negatives
- [ ] **Write up results section** — use evaluation_by_query_type.csv as the primary table
- [ ] **Include UMAP figure** — umap_baseline_vs_ft.png as a qualitative figure
- [ ] **Include cosine similarity table** — biomedical_term_similarity.csv shows embedding space improvement

---

## Expected Results After Full Training

Based on the current 1-epoch, 1,998-pair run showing MRR 0.709, after proper training:

| Metric | Current (1 epoch, 2K pairs) | Expected (3 epochs, 6K pairs) |
|---|---|---|
| Avg MRR | 0.709 | 0.73–0.78 (estimated) |
| vs PubMedBERT gap | +12.4% | +18–25% (estimated) |
| vs BioBERT gap | +15.5% | +20–28% (estimated) |

These are estimates based on typical MNRL convergence curves. Actual results may vary.
